spring:
  application:
    name: nacos-product-server
  cloud:
    nacos:
      config:
        server-addr: ${server-addr}:8848
        file-extension: yaml
      discovery:
        server-addr: ${server-addr}:8848
      username: nacos
      password: Ethan_970311
server:
  port: 8301
#feign:
#  hystrix:
#    enabled: true
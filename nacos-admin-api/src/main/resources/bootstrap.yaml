spring:
  application:
    name: nacos-admin-api
  cloud:
    nacos:
      config:
        server-addr: ${server-addr}:8848
        file-extension: yaml
      discovery:
        server-addr: ${server-addr}:8848
      username: nacos
      password: Ethan_970311
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true
server:
  port: 8202
#feign:
#  hystrix:
#    enabled: true
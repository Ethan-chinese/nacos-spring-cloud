spring:
  application:
    name: nacos-admin-api
  cloud:
    nacos:
      config:
        server-addr: ${server-addr}
        file-extension: yaml
      discovery:
        server-addr: ${server-addr}
      username: nacos
      password: Ethan_970311
server:
  port: 8202
#feign:
#  hystrix:
#    enabled: true
spring:
  application:
    name: nacos-user-server
  cloud:
    nacos:
      config:
        server-addr: ${server-addr}
        file-extension: yaml
      discovery:
        server-addr: ${server-addr}
      username: nacos
      password: Ethan_970311
server:
  port: 8201
#feign:
#  hystrix:
#    enabled: true
spring:
  application:
    name: nacos-order-server
  cloud:
    nacos:
      config:
        server-addr: ${server-addr}:8848
        file-extension: yaml
      discovery:
        server-addr: ${server-addr}:8848
      username: nacos
      password: Ethan_970311
server:
  port: 8401
#feign:
#  hystrix:
#    enabled: true